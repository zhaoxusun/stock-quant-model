{
  "buildCommand": "rm -rf xgb_pkgs && uv pip install numpy --no-binary numpy --no-deps -t xgb_pkgs && uv pip install xgboost --no-deps -t xgb_pkgs && python3 cleanup_vercel.py",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
