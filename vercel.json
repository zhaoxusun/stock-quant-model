{
  "buildCommand": "pip install xgboost --no-deps -t xgb_pkgs && pip install akshare baostock --no-deps && python3 cleanup_vercel.py",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
