{
  "buildCommand": "uv pip install xgboost --no-deps -t xgb_pkgs && uv pip install akshare baostock --no-deps -t xgb_pkgs && python3 cleanup_vercel.py",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
