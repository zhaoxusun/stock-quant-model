{
  "env": {
    "UV_COMPILE_BYTECODE": "0",
    "UV_NO_CACHE": "1"
  },
  "buildCommand": "uv pip install xgboost --no-deps -t xgb_pkgs && python3 cleanup_vercel.py",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
