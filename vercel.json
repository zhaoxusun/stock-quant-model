{
  "buildCommand": "echo 'cache-buster: 181a141-size' && uv pip install xgboost --no-deps -t xgb_pkgs && python3 cleanup_vercel.py",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
